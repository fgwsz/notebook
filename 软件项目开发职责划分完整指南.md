# 软件项目开发职责划分完整指南

> 本指南基于“逻辑与实现分离”的核心思想，总结了一套适用于任何软件项目（游戏、Web应用、工具脚本、系统软件）的职责划分框架。


## 第一部分：核心认知——两层思维模型

### 1.1 编程思维的两大分野

| 思维模式 | 适用场景 | 关注点 | 评价标准 |
| :--- | :--- | :--- | :--- |
| **算法脑** | 算法、引擎、数据库内核 | 内存布局、缓存命中率、指令流水线 | 客观：跑得快就是快 |
| **业务脑** | 业务系统（支付/订单/ERP） | 状态流转、边界条件、数据一致性 | 主观：改得动、不出错、新人能看懂 |

### 1.2 核心原则

> **写代码不是寻找真理，而是维持并优化“需要与被需要”的复杂关系网络。**

- 软件生态胜出的原因不是“理论上更好”，而是“在人际关系、商业博弈和时间成本上形成了不得不选的闭环”
- 领域知识是“免死金牌”，技术框架来来去去，领域知识永不贬值
- 真正的掌控感，是“手里有剑，但心中无剑”——将技术细节从显意识压入潜意识


## 第二部分：物理结构——标准项目目录（以Python为例）

```
my_project/
├── .venv/                     # 虚拟环境（自动生成）
├── src/
│   └── my_project/            # 核心包名
│       ├── __init__.py
│       │
│       ├── core/              # 【业务层】纯业务逻辑
│       │   ├── entities/      # 实体类（业务对象）
│       │   ├── rules/         # 业务规则
│       │   ├── ports/         # 抽象接口（依赖倒置）
│       │   └── services/      # 领域服务（纯业务计算）
│       │
│       ├── application/       # 【逻辑层】用例编排
│       │   └── use_cases/     # 业务流程编排
│       │
│       ├── infrastructure/    # 【实现层】外部依赖
│       │   ├── persistence/   # 数据库/文件存储
│       │   ├── network/       # HTTP/API调用
│       │   ├── renderer/      # 图形/UI渲染
│       │   └── config/        # 配置管理
│       │
│       ├── api/               # 【接口层】对外暴露
│       │   ├── routes/        # REST/CLI入口
│       │   └── schemas/       # 请求/响应模型
│       │
│       └── utils/             # 【工具层】纯技术辅助
│           ├── logger.py      # 日志
│           ├── time_helpers.py
│           └── crypto.py
│
├── tests/                     # 单元测试
├── pyproject.toml             # 项目配置
├── requirements.txt
└── README.md
```


## 第三部分：各层职责详解

### 3.1 业务层（`core/`）——“大脑”

**定义**：规定“这个世界应该怎么运转”。项目存在的唯一理由，是所有业务规则的集合。

**特征**：
- 极度稳定，不随技术选型变化
- 不依赖任何外部系统（无IO、无网络、无数据库）
- 只导入：`core`内部模块、`abc`（抽象基类）、`typing`（类型提示）

**内容**：
| 子目录 | 职责 | 示例 |
| :--- | :--- | :--- |
| `entities/` | 业务实体（属性+纯内存计算） | `Monster.take_damage()` |
| `rules/` | 业务规则 | `增值税率 = 13%` |
| `ports/` | 抽象接口（定义“我需要什么”） | `class GameRepository(ABC)` |
| `services/` | 领域服务（纯业务计算） | `def calc_discount(amount, vip_level)` |

### 3.2 逻辑层（`application/`）——“神经”

**定义**：编排业务流程——先做A，再做B，最后做C。不制定规则，只执行流程。

**特征**：
- 变化频繁，跟随需求变动
- 负责协调多个`core`模块
- 调用`infrastructure`实现具体操作

**内容**：
- `use_cases/`：每个文件对应一个用户故事
- 示例：`battle_service.fight()` → 查数据库（调infra）→ 扣血（调core）→ 保存（调infra）

### 3.3 实现层（`infrastructure/`）——“手脚”

**定义**：具体的“怎么做”。所有与外部世界打交道的技术细节。

**特征**：
- 可以随时替换（换数据库、换图形引擎、换云服务）
- 实现`core/ports/`中定义的接口
- 依赖具体的第三方库

**内容**：
| 子目录 | 职责 | 示例 |
| :--- | :--- | :--- |
| `persistence/` | 数据持久化 | `JsonGameRepository.save_game()` |
| `network/` | 网络通信 | `requests.get()` |
| `renderer/` | 图形渲染 | `pygame.draw.circle()` |
| `config/` | 环境配置 | 读取`.env`文件 |

### 3.4 接口层（`api/`）——“嘴”

**定义**：对外暴露的入口，接收请求，返回响应。

**职责**（只做三件事）：
1. 解析输入参数
2. 调用`application`或`core`
3. 封装返回结果

**铁律**：绝对不在`api/`里写任何业务判断。

### 3.5 工具层（`utils/`）——“工具箱”

**定义**：与业务无关、可跨项目复用的纯技术辅助函数。

**特征**：
- 极度稳定，不依赖项目内任何其他模块
- 只依赖标准库或第三方PyPI包
- 无状态，纯函数

**内容**：
- 日志配置
- 时间格式化
- 加密解密
- 重试/超时装饰器


## 第四部分：依赖流向——金字塔原则

### 4.1 依赖方向（从上到下调用，绝对禁止反向）

```
            [api/]              ← 依赖：application, core, infrastructure, utils
               ↓
       [application/]           ← 依赖：core, infrastructure, utils
               ↓
    ┌──────────┴──────────┐
    ↓                     ↓
  [core/]         [infrastructure/]   ← core定义接口，infrastructure实现接口
    ↓                     ↓
    └──────────┬──────────┘
               ↓
            [utils/]               ← 最底层，谁都不依赖
```

### 4.2 核心铁律

1. **core 绝对不导入**：`infrastructure`、`api`、`requests`、`sqlalchemy`、`json`、`open()`、`print()`
2. **utils 绝对不导入**：`core`、`application`、`infrastructure`
3. **infrastructure 实现 core 的接口**：不直接调用，而是“被调用”
4. **依赖倒置**：高层模块（core）不依赖低层模块（infrastructure），两者都依赖抽象（接口）


## 第五部分：业务层 vs 逻辑层 vs 实现层——深度辨析

### 5.1 核心区别

| 概念 | 别名 | 回答的问题 | 类比 |
| :--- | :--- | :--- | :--- |
| **业务层** | 领域层（Domain） | “能做什么”（What） | 立法院（制定规则） |
| **逻辑层** | 应用层（Application） | “按什么顺序做”（How & When） | 执法局（编排流程） |
| **实现层** | 基础设施层（Infrastructure） | “用什么工具做”（With What） | 施工队（具体干活） |

### 5.2 决策判断标准

**“业务层是否该关心这件事？”用三个问题来判断：**

1. **这是业务规则还是技术细节？**
   - 业务规则 → 进 `core/`
   - 技术细节 → 进 `infrastructure/`

2. **如果换一种技术实现，这个逻辑需要改吗？**
   - 不需要改 → 进 `core/`
   - 需要重写 → 进 `infrastructure/`

3. **代码里出现了外部依赖吗？（数据库、网络、文件、图形、终端输入）**
   - 出现了 → 只写接口（ports），实现放在 `infrastructure/`
   - 没出现（纯计算/判断） → 直接写在 `core/`


## 第六部分：实战案例——游戏结束存档

### 6.1 职责分布

| 层级 | 文件 | 代码示例 | 职责 |
| :--- | :--- | :--- | :--- |
| 业务层 | `core/ports/repository.py` | `class GameRepository(ABC): @abstractmethod def save_game()` | 定义“需要有存档功能”这个契约 |
| 业务层 | `core/services/game_engine.py` | `self.repo.save_game(monsters, hp)` | 业务决策：游戏结束必须存档 |
| 实现层 | `infrastructure/json_repo.py` | `with open("save.json","w") as f: json.dump(...)` | 具体写JSON文件 |
| 实现层 | `infrastructure/cloud_repo.py` | `s3.upload_file(...)` | 具体上传云端 |

### 6.2 污染检测清单

**如果在 `core/` 下看到以下任何一项，就是“污染”，必须重构：**

- `open(` / `print(`
- `import sqlite3` / `import json` / `import yaml`
- `requests.get(` / `socket.`
- `pygame.` / `matplotlib.`
- 导入了 `infrastructure` 或 `utils/file_io.py`


## 第七部分：跨语言策略——Python与Rust的协作

### 7.1 定位

| 语言 | 定位 | 原因 |
| :--- | :--- | :--- |
| **Python** | 快速验证领域想法的“草稿纸” | 编码处理优雅、迭代速度快、语法简洁 |
| **Rust** | 交付确定性的“正式蓝图” | 内存安全、无畏并发、零成本抽象 |

### 7.2 策略

1. **用Python写原型**：只关注业务逻辑是否正确，不关心性能
2. **用Rust重写热路径**：把 `core/` 中的纯逻辑翻译成Rust的 `fn`
3. **FFI作为桥梁**：Rust编译成动态库（`.so`/`.dll`），Python用 `ctypes` 或 PyO3 调用
4. **变化隔离**：Python的 `infrastructure/` 层处理IO和外部交互，Rust只负责纯计算

### 7.3 收益

- 换图形引擎：只改 `infrastructure/renderer.py`
- 换数据库：只改 `infrastructure/persistence/`
- 换业务规则：只改 `core/`
- 性能瓶颈：用Rust重写 `core/` 并暴露FFI

**无论怎么换，其他层代码纹丝不动。**


## 第八部分：环境管理（Python）

### 8.1 极简流程（仅用Python内置工具）

```bash
# 1. 创建虚拟环境
python -m venv .venv

# 2. 激活环境
# Windows: .venv\Scripts\activate
# Mac/Linux: source .venv/bin/activate

# 3. 安装依赖
pip install -r requirements.txt

# 4. 导出依赖
pip freeze > requirements.txt
```

### 8.2 铁律

> **只要新建Python项目文件夹，第一件事就是执行 `python -m venv .venv`。**


## 第九部分：终极原则——检查清单

### 9.1 代码放置自查表

| 问题 | 是→放这 | 否→往下问 |
| :--- | :--- | :--- |
| 这是纯业务规则/计算？（不涉及任何IO） | `core/` | ↓ |
| 这是业务流程编排？（调用多个core模块） | `application/` | ↓ |
| 这是与外部世界打交道？（数据库/网络/文件/图形） | `infrastructure/` | ↓ |
| 这是对外接收请求或返回响应？ | `api/` | ↓ |
| 这是与技术无关的纯辅助函数？ | `utils/` | ↓ |

### 9.2 交叉依赖检查

- ✅ `api` → `application` / `infrastructure`
- ✅ `application` → `core` / `infrastructure`
- ✅ `core` → `utils`
- ✅ `infrastructure` → `core`（实现接口）
- ❌ `core` → `application` / `infrastructure`
- ❌ `utils` → `core`
- ❌ `infrastructure` 之间不该互相依赖（如果必须，通过接口解耦）

### 9.3 稳不稳定原则

```
稳定的业务层（core）    —— 几乎不变
      ↑
多变的逻辑层（application）—— 随需求变化
      ↑
易变的实现层（infrastructure）—— 随技术选型变化
```


## 第十部分：核心心法

1. **领域知识是护城河，技术框架是工具。** 技术会迭代，领域规则百年不变。
2. **不要用战术的勤奋掩盖战略的懒惰。** 在业务系统里“抠内存”是炫技，把状态机理清楚才是真功夫。
3. **依赖倒置是解耦的灵魂。** 高层定义“我需要什么”，低层实现“我如何提供”。
4. **写业务代码时，忘掉内存；写核心热路径时，唤醒你的“算法脑”。** 两种思维在不同层级切换。
5. **“逻辑层和实现层的完全分离”是软件工程的最高准则。** 它让代码拥有“抗衰老”能力。

---

> 📌 **本指南适用于任何编程语言和任何软件类型。**
> 无论是游戏、Web应用、工具脚本还是系统软件，只要遵循“业务逻辑不依赖技术细节”这一核心原则，代码就能保持长久的可维护性和可扩展性。

## 第十一部分：评价
这份指南，其核心思想——“业务逻辑与技术实现分离”——并非凭空创造，而是被众多顶尖项目验证过的、软件工程领域最核心的设计原则之一。

我们可以将这份指南看作这些成功架构的一个具体化、可操作的实现版本。为了更直观地理解，下面用几个知名项目来印证你的指南：

| 指南 | Linux 内核 | Spring Framework | React |
| :--- | :--- | :--- | :--- |
| **`core/` (业务层)** | 核心内核（进程调度、内存管理） | 核心容器（IoC、DI） | React Core (组件定义API) |
| **`application/` (逻辑层)** | 系统调用接口 (SCI) | AOP、数据访问层、Web层 | Reconciler (协调器) |
| **`infrastructure/` (实现层)** | 体系结构相关代码 (BSP) | 具体数据库实现、Web服务器实现 | Renderer (渲染器) |
| **`api/` (接口层)** | 系统调用接口 (SCI) | Spring MVC的Controller | 用户界面 (UI) |
| **`utils/` (工具层)** | 内核中通用的库函数 | Spring中的工具类（如 `StringUtils`） | `shared` 目录 |

### 🏗️ Linux 内核：最硬核的分层实践

Linux 内核本身就是分层思想的典范。它通过**系统调用接口（SCI）**将用户空间的应用程序与内核核心逻辑隔离开来。内核核心处理进程、内存等核心任务，而**板级支持包（BSP）** 则负责处理与具体硬件相关的代码。这完美对应了你的指南中“业务层（`core/`）”与“实现层（`infrastructure/`）”的分离，甚至定义了“接口层（`api/`）”。

### 🏗️ Spring Framework：企业级应用的解耦典范

Spring 通过其**控制反转（IoC）** 和**依赖注入（DI）** 容器，成为实现“依赖倒置”的标杆。开发者只需定义“我需要什么服务”（抽象接口），Spring 容器在运行时将具体的实现（如 `MySQL` 或 `PostgreSQL` 驱动）“注入”进来。这正是你在**业务层（`core/`）** 定义 `ports/`（接口），在**实现层（`infrastructure/`）** 提供具体实现的完美实践。

### 🏗️ React：UI 框架中的“逻辑与实现”分离

React 16 之后的 **Fiber 架构** 是分层思想的绝佳案例，它清晰地分为三层：
1.  **Scheduler（调度器）**：决定何时执行任务。
2.  **Reconciler（协调器）**：**核心逻辑层**，负责找出哪些组件发生了变化。这部分逻辑是平台无关的。
3.  **Renderer（渲染器）**：**实现层**，负责将 Reconciler 找出的变化“画”到不同平台上（DOM、Native、Canvas 等）。

这种分层让 React 可以将同一套核心逻辑（Reconciler）复用到 Web、移动端（React Native）和 VR（React VR）等不同平台。

### 🏗️ 更多案例印证

*   **Alibaba COLA**：这套应用架构的最佳实践，明确提倡“**以业务为核心，解耦外部依赖，分离业务复杂度和技术复杂度**”。这与你的指南核心思想完全一致。
*   **AI 知识库项目**：采用**六边形架构**，将系统清晰地划分为“**核心领域层、应用用例层、外部适配层**”。这种架构使其能快速适配不同数据库和认证系统，且**新增功能无需修改核心代码**。
*   **DDD 项目模板**：领域驱动设计（DDD）的分层架构，其“**领域层（`domain/`）**”和“**基础设施层（`infrastructure/`）**”的划分，与你指南中的 `core/` 和 `infrastructure/` 在概念上几乎完全一致。
*   **Redis**：高性能的 Redis 也采用了典型的分层架构，例如其**网络部分就分了四层**，每一层都职责明确。
*   **TensorFlow**：其架构也明确分为**应用层、API 接口层和 TensorFlow 核心层**。其核心层（图计算等）用 C++ 实现以保证高性能，而应用层（如 Keras）则用 Python 提供易用接口。这再次印证了“核心逻辑”与“外部交互”分离的普适性。

### 💎 总结

这份指南，与诸多知名项目所体现的设计哲学高度一致。它的核心价值在于，为开发者提供了一套清晰、可落地的实践方法，来管理软件固有的复杂性。

现在你再看这些知名项目时，它们将不再是难以理解的代码堆砌，而是一个个鲜活的、可拆解的架构范本。希望这份来自真实世界的印证，能让你对自己的总结更有信心。
